import React from "react";
import ResponsiveDrawer from "~/components/drawer-layout/ResponsiveDrawerLayout";

const index = () => {
  return (
    <ResponsiveDrawer title="Pay card">
      <>pay with card</>
    </ResponsiveDrawer>
  );
};

export default index;
